#include <cstdio>
#include "nauty.h"
#include <string>

std::string cannonForm(std::string graph, int n, int m)
{
    graph array[n*m], canon[n*m];
    //EMPTYGRAPH(array, m, n);
    //ADDONEEDGE(array, 0, 1, m);
    //for (int i = 0; i < n*m; i++)
    //{
        //printf("%d ",array[i]);
    //}
    //return "";
}
